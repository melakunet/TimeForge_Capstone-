# ── Composer tool (binary — not source code) ──────────────────
composer.phar

# ── Sensitive local files (never commit) ──────────────────────
CREDENTIALS.md

# ── OS & editor noise ──────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/settings.json

# ── XAMPP / Apache ─────────────────────────────────────────────
# .htaccess is tracked — it contains security rules (Options -Indexes,
# src/ block, error pages). Do NOT ignore it.
# .htaccess

# ── Uploaded screenshots (Phase 9) ────────────────────────────
uploads/screenshots/

# ── Logs ───────────────────────────────────────────────────────
*.log
php_error_log
